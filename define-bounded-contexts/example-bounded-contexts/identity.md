<!--
Bounded Context Canvas - one context, discovered from code
Generated by The Agentic Architect (define-bounded-contexts)
Illustrative sample for the fictional ShopFlow system. Format: the ddd-crew Bounded Context Canvas (CC BY 4.0).
-->

# Bounded Context Canvas - Identity

- Repository: github.com/example/shopflow
- Generated: 2026-06-02
- Generated by: The Agentic Architect (define-bounded-contexts)
- Confidence: medium
- Paths: `src/shared/auth/` (evidence: src/shared/auth/)

[Back to index](../example-bounded-contexts.md)

## Purpose

Authenticate users and resolve who a `Customer` or `Merchant` is, so other contexts can act on behalf of a known actor. Serves every other context as a supporting capability. (evidence: src/shared/auth/IdentityApi.kt)

## Strategic Classification

| Domain | Business Model | Evolution |
| --- | --- | --- |
| generic | compliance enforcer | commodity |

- Domain rationale: authentication is a common, off-the-shelf-style capability, not a differentiator (evidence: src/shared/auth/Session.kt)

## Domain Roles

- gateway - resolves and authenticates actors for other contexts (evidence: src/shared/auth/IdentityApi.kt)

## Inbound Communication

Collaborations initiated by others. Message types: command (do something), query (ask for information), event (something happened).

| Message | Type | Collaborator | Relationship type |
| --- | --- | --- | --- |
| Log in | command | direct user (web frontend) | (frontend-owned UI) (evidence: src/shared/auth/AuthController.kt) |
| Resolve current customer | query | Sales | Open Host Service (U) (evidence: src/shared/auth/IdentityApi.kt) |
| Resolve customer address | query | Delivery | Conformist (U) (evidence: src/delivery/Shipment.kt) |

## Outbound Communication

Collaborations initiated by this context.

| Message | Type | Collaborator | Relationship type |
| --- | --- | --- | --- |
| none found | | | |

## Ubiquitous Language

Key domain terms in this context and what they mean (verbatim from the code).

| Term | Meaning |
| --- | --- |
| `User` | An authenticated account (evidence: src/shared/auth/User.kt) |
| `Session` | An active authenticated period for a user (evidence: src/shared/auth/Session.kt) |
| `Customer` | A user who buys (evidence: src/shared/Customer.kt) |
| `Merchant` | A user who sells through ShopFlow (evidence: src/shared/Merchant.kt) |

## Business Decisions

Key business rules and policies enforced here.

- A request without a valid `Session` is unauthenticated and rejected (evidence: src/shared/auth/Session.kt)
- > TODO (human input needed): confirm session expiry and password policy.

## Assumptions

- > TODO (human input needed): confirm whether `Customer`/`Merchant` should truly belong to Identity or be a shared kernel.

## Verification Metrics

Metrics that would reveal whether this boundary is a good fit (CI/CD, issue tracker, live system).

- > TODO (human input needed): define fitness metrics, e.g. auth failure rate.

## Open Questions

- Identity lives under `src/shared/` and mixes auth with the shared `Customer`/`Merchant` model - is it a true context or a shared kernel? (evidence: src/shared/Customer.kt, src/shared/auth/)

---

## Owned data

`identity` schema (`users`, `sessions` tables) (evidence: db/migrations/V1__users.sql)
