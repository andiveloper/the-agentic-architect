<!--
Commit History Analysis - diagnosed from git history
Generated by The Agentic Architect (analyze-commits)
Replace every <...> placeholder. Keep the raw lists compact (file + count). The verdict and the highest-risk cross-reference are the point.
-->

# Commit History Analysis - <System Name>

- Repository: <repo identifier or path>
- Generated: <YYYY-MM-DD>
- Generated by: The Agentic Architect (analyze-commits)
- Method: [The Git Commands I Run Before Reading Any Code](https://piechowski.io/post/git-commands-before-reading-code/) by Ally Piechowski

> A diagnostic read of the **git history** before reading any code: where to start, what to be careful of. Every number comes from a `git log` / `git shortlog` command; see "Data caveats" for what limits confidence.

---

## Verdict (read this first)

The 3-5 things to know before opening the code:

- **Read first:** <file(s)> - <why: high-churn and high-bug / the file everyone warns about>.
- **Bus factor:** <e.g. 1 - `author` wrote 68% of commits and last committed 8 months ago>.
- **Velocity:** <e.g. steady ~120 commits/month / declining since <month> / halved in <month>>.
- **Firefighting:** <e.g. 12 reverts/hotfixes in the last year - the team doesn't trust its deploys / none found>.
- **Caveats:** <e.g. squash-merge compresses authorship; bug clustering is weak due to sparse messages>.

---

## Highest-risk files (churn x bugs)

Files that are **both** high-churn and high-bug - they keep breaking and keep getting patched but never properly fixed. Start here.

| File | Churn rank | Bug rank |
| --- | --- | --- |
| <path> | <#> | <#> |
| <path> | <#> | <#> |

> <If the intersection is empty, say so and point to the top churn file instead.>

---

## 1. Churn hotspots - what changes the most

Most-changed files in the last year (from `<source dir>`).

| Rank | Changes | File |
| --- | --- | --- |
| 1 | <count> | <file> |
| 2 | <count> | <file> |
| ... | ... | ... |

<One line of interpretation: which of these are active development vs. unowned drag.>

---

## 2. Contributors & bus factor - who built this

| Contributor | Commits | Share | Active last 6 months? |
| --- | --- | --- | --- |
| <author> | <count> | <pct>% | <yes/no> |
| <author> | <count> | <pct>% | <yes/no> |

<Interpretation: bus factor, whether builders are still maintainers, the active tail. Note squash-merge if detected.>

---

## 3. Bug clusters - where bugs keep getting patched

Files most frequently touched by `fix`/`bug`/`broken` commits.

| Rank | Bug-fix touches | File |
| --- | --- | --- |
| 1 | <count> | <file> |
| ... | ... | ... |

<Interpretation, plus the message-discipline caveat if relevant.>

---

## 4. Velocity - accelerating or dying

Commits per month over the project's history.

| Month | Commits |
| --- | --- |
| <YYYY-MM> | <count> |
| ... | ... |

> <Shape: steady / declining / spiky-batched. Call out any month where the count halved and what it might mean.>

---

## 5. Firefighting - crisis patterns

`revert` / `hotfix` / `emergency` / `rollback` commits in the last year: **<count>**.

- <subject line> (<date>)
- <subject line> (<date>)

<Interpretation: normal background vs. a deploy-trust problem. Note if zero could mean stable OR poor messages.>

---

## Data caveats

What limits the confidence of the above:

- <Squash-merge workflow: authorship reflects who merged, not who wrote.>
- <Commit-message discipline: ~<pct>% of subjects are non-descriptive, weakening bug clustering.>
- <Shallow clone / history starts <date>: older history is missing.>
- <Source dir for churn: <path> (chosen because ...); lockfiles/generated files were filtered out.>
