<!--
Ubiquitous Language - discovered from code
Generated by The Agentic Architect (discover-ubiquitous-language)
This is an illustrative sample for a fictional commerce/delivery system.
-->

# Ubiquitous Language - ShopFlow

- Repository: github.com/example/shopflow
- Generated: 2026-06-02
- Generated by: The Agentic Architect (discover-ubiquitous-language)

> This is the domain language **as the code currently uses it**. Review it with domain experts and ask "is this what these words mean to you?" - the goal is to find gaps and misunderstandings between code and domain. Terms are kept verbatim from the code; mismatches are intentional discussion material.

---

## Domain classification

Rough grouping of the discovered terms into candidate domain areas (for discussion, not a rigorous bounded-context map).

- **Orders**: `Order`, `OrderLine`, `custOrd`, `Backordered`, `OrderPlaced`
- **Delivery**: `DeliveryNote`, `Shipment`, `Courier`, `Dispatch`, `POD`
- **Billing**: `Invoice`, `Settle`, `Refund`, `Tariff`
- **Shared / cross-cutting**: `Customer`, `Merchant`

---

## Ubiquitous Language

Each term is recorded exactly as it appears in the code; each definition is one plain-language sentence describing its apparent domain meaning.

| Term | Definition |
| --- | --- |
| `Order` | A customer's request to buy one or more products. (evidence: src/orders/Order.kt) |
| `OrderLine` | A single product and quantity within an order. (evidence: src/orders/OrderLine.kt) |
| `custOrd` | Appears to be an abbreviation of customer order used in the legacy import. (evidence: src/legacy/import.kt) |
| `Backordered` | An order state where stock is unavailable and fulfilment waits for restock. (evidence: src/orders/OrderStatus.kt) |
| `OrderPlaced` | Event signalling a customer has submitted an order. (evidence: src/orders/events/OrderPlaced.kt) |
| `DeliveryNote` | Document accompanying a shipment that lists its contents. (evidence: src/delivery/DeliveryNote.kt) |
| `Shipment` | A batch of goods physically sent to a customer. (evidence: src/delivery/Shipment.kt) |
| `Courier` | External party that physically transports a shipment. (evidence: src/delivery/Courier.kt) |
| `Dispatch` | The act of handing a shipment to a courier. (evidence: src/delivery/DispatchService.kt) |
| `POD` | > TODO (human input needed): Is `POD` "proof of delivery" or "point of delivery"? Both readings appear. (evidence: src/delivery/Shipment.kt) |
| `Invoice` | A request for payment issued for an order. (evidence: src/billing/Invoice.kt) |
| `Settle` | Mark an invoice as fully paid. (evidence: src/billing/InvoiceService.kt) |
| `Refund` | Return money to a customer for a cancelled or returned order. (evidence: src/billing/Refund.kt) |
| `Tariff` | A pricing rule applied to a delivery based on weight or zone. (evidence: src/billing/Tariff.kt) |
| `Customer` | A person or organisation that places orders. (evidence: src/shared/Customer.kt) |
| `Merchant` | A seller whose products are sold through the platform. (evidence: src/shared/Merchant.kt) |

---

## Discussion points / possible gaps

Things to clarify with domain experts:

- **Synonyms**: `DeliveryNote` and `Shipment` are used almost interchangeably in `DispatchService` - are they the same thing or distinct? (evidence: src/delivery/DispatchService.kt)
- **Conflicting use**: `custOrd` in the legacy import vs `Order` elsewhere appear to be the same concept under two names. (evidence: src/legacy/import.kt, src/orders/Order.kt)
- **Unclear abbreviation**: `POD` - meaning not determinable from code (see TODO above). (evidence: src/delivery/Shipment.kt)
- **Undetermined meaning**: see the `TODO (human input needed)` entry in the glossary above.

---

## Provenance

- Terms derived from repository evidence: 16
- Terms with unresolved TODOs (need expert input): 1 (`POD`)
